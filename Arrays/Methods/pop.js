const arr = [1, 2, 3, 4, 5];
const delElm = arr.pop();
console.log(arr);
console.log(delElm);

// pop() removes the last element of an array and returns it. 
// Pop() make changes in the original array.